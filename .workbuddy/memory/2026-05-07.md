# 2026-05-07

- 继续 One 仓库复核队列维护：给 `scripts/generate_review_queue.py` 增加 `reason_distribution`，统计缺中文、重复风险、加载错误、低质量分、待审等主要原因，并在 CLI 输出前几类分布。
- 页面 `index.html` 的复核队列开始展示原因分布；`scripts/validate_data.py` 增加 `review_queue.json.reason_distribution` 结构校验；补充 `tests/test_generate_review_queue.py` 和 `tests/test_validate_data.py`。
- 已重新生成 `data/review_queue.json`，当前 24 项全部主要原因是“缺少中文标签”。验证通过：`python -m unittest discover -s tests`（43 tests OK）、页面内联脚本 `node --check`、`python scripts/validate_data.py --data-dir data`。
